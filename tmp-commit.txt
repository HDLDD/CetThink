fix(persist): 修「清空数据被旧副本复活」与「深恢复按体量取胜」两个 P0

- persist.pickBest()：选副本改为**先比 updatedAt**，同刻再比痕迹分。
  旧实现只比 scoreRaw → 「清空全部数据」写出的是空状态（分低），
  残留的旧 snapshot/IDB 分更高，刷新后旧数据原地复活。
- 新增 clearAllPersisted()：main/backup/snapshot/last_save/save_count + IDB delete
  只清 main/backup 不够 —— 这正是复活源头。
- store.clearAll()：连作用域键（safeStorage 前缀）与裸键（pm_*/last_visit/
  favorite_meta/achievements/位置键）一起清，并重置为 defaultState。
- store.hydrateFromDeepStorage()：改为只认 updatedAt 更新的副本（原先比 vocab 长度+分钟）。
- savePersisted() 返回写入结果、写盘失败不再更新"上次保存时间"（配额打满时不再谎报已保存）。
- SettingsPage：清空按钮改调 store.clearAll()，提示改为"已彻底清空（含快照与 IndexedDB）"。
- tests/persist.test.mjs：12 项纯函数回归（旧的"大"副本 vs 新的空状态必须选空状态），已接入 npm test。

真机回归（Redmi/Android16，adb+CDP）7/7：
  清空前 main=20 词 → 清空后 0 → 刷新后仍 0 → IDB 深恢复也未带回；快照被重建但内容为空。
